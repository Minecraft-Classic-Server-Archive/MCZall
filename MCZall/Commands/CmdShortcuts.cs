using System;

//TODO: Read from Txt

namespace MCZall.Commands
{
    /*
    public class CmdShortcuts : Command
    {
        public override string name { get { return "shortcuts"; } }
        public CmdShortcuts() { }
        public override void Use(Player p, string message)
        {
            if (message != "") { Help(p); return; }
            message = message + "a(abort), ";
            message = message + "b(about), ";
            message = message + "c(copy), ";
            message = message + "e(spheroid), ";
            message = message + "f(fill), ";
            message = message + "g(goto), ";
            message = message + "k(kick), ";
            message = message + "l(lava), ";
            message = message + "o(portal), ";
            message = message + "p(paint), ";
            message = message + "r(replace), ";
            message = message + "s(summon), ";
            message = message + "u(undo), ";
            message = message + "v(paste), ";
            message = message + "w(water), ";
            message = message + "x(cut), ";
            message = message + "z(cuboid), ";
            message = message + "bi(banip), ";
            message = message + "kb(kickban), ";
            message = message + "rn(replacenot), ";
            message = message + "rp(restartphysics), ";
            message = message + "zm(megaboid), ";
            message = message + "rank(setrank)";

            p.SendMessage(message);
        }

        public override void Help(Player p) {
            p.SendMessage("/shortcuts - Lists every shortcut available");
        }
    }
     */
}
